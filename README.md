<div align="center">

<img src="assets/icon.png" width="120" alt="SkipKOB_Helper">

# SkipKOB_Helper

**Приложение для учёта прогулов в университете**

Тёмная тема · Android и Windows · всё хранится локально

<img src="https://img.shields.io/badge/Dart-Flutter%203.29-02569B?logo=flutter&logoColor=white" alt="Flutter">
<img src="https://img.shields.io/badge/platform-Android%20%7C%20Windows-6E56CF" alt="Platform">

### [⬇ Скачать последний релиз](https://github.com/Arkorij/SkipKOB_helper/releases/latest)

APK для Android · архив для Windows

</div>

---

Отмечаешь, был ли ты на паре, — приложение считает статистику: сколько прогулов,
сколько из них по уважительной причине, сколько дней пропущено полностью и по каким
предметам пропускаешь чаще. Отмечать можно и в течение дня, и постфактум.

## Скриншоты

<div align="center">
<table>
<tr>
<td align="center"><b>Статистика</b></td>
<td align="center"><b>Расписание</b></td>
<td align="center"><b>Настройки</b></td>
</tr>
<tr>
<td><img src="docs/screenshots/01-stats.png" width="260" alt="Статистика"></td>
<td><img src="docs/screenshots/02-schedule.png" width="260" alt="Расписание"></td>
<td><img src="docs/screenshots/03-settings.png" width="260" alt="Настройки"></td>
</tr>
</table>
</div>

## Возможности

### 📊 Статистика
- Счётчики: всего прогулов, полных пропущенных дней, уважительные / неуважительные.
- Кольцевая диаграмма причин — похожие причины различаются оттенками.
- Прогулы по предметам в двух режимах: **суммарно** и **детально** (отдельно лекции,
  практики и лабораторные по каждому предмету).
- Отдельный блок «не считается прогулом»: освобождён от пары, отменённые пары,
  посещённые консультации.

### 📅 Расписание
- Учебные недели **1–40**, чётные и нечётные с разным базовым расписанием.
- У каждой пары цветной значок типа: <kbd>Лекция</kbd> <kbd>Практика</kbd>
  <kbd>Лабораторная</kbd> <kbd>Консультация</kbd>.
- Статус для каждой пары: пришёл · справка · уваж. причина · проспал · не захотел ·
  нет сил · всё сдал · пара отменена.
- Смена недели: кнопками, выбором из сетки или **протягиванием списка за край** —
  список тянется за пальцем, индикатор заполняется по мере оттягивания.
- Изменения расписания на конкретный день, не ломая базовое расписание.
- Последняя открытая неделя запоминается.

### ⚙️ Настройки
- Учебный год (по 1 сентября строится весь календарь).
- **Каникулы** — на них нумерация недель останавливается.
- Редактор базового расписания на чётную и нечётную неделю.
- Экспорт / импорт расписания (`.conf` / `.json`).
- Экспорт отчёта о прогулах в Markdown — удобно и читать, и скормить ИИ для анализа.
- **Свайп между страницами** — включён, отключается тумблером.
- Полный сброс с предупреждением и таймером (защита от случайного нажатия).

## Как считается «полный пропущенный день»

Учитываются только «зачётные» пары — без консультаций, отменённых пар и «всё сдал».

| Ситуация | Результат |
|---|---|
| Все пары отменены / всё сдано | день **не** пропущен |
| Хоть на одной паре «пришёл» | день **не** пропущен |
| Все зачётные пары пропущены (любая причина) | день пропущен **полностью** |
| Половина отменена, половина прогуляна | день пропущен **полностью** |

## Формат расписания

Пары задаются по одной в строке. Префиксы в начале строки:

| Префикс | Значение |
|---|---|
| *(нет)* | лекция |
| `*` | практика |
| `~` | лабораторная |
| `#` | консультация |

Префиксы комбинируются: `#~ Физика` — консультация по лабораторной.

Файл импорта (`.conf` / `.json`) содержит `academic_year_start`, `breaks` и
`base_schedule`. Импорт перезаписывает базовое расписание, **не трогая** уже
проставленные отметки. Пример готового файла — [`schedule_SO211KOB.conf`](schedule_SO211KOB.conf).

## Установка

**Android** — скачать APK из [релизов](https://github.com/Arkorij/SkipKOB_helper/releases/latest),
открыть на телефоне и разрешить установку из неизвестных источников.

**Windows** — скачать архив, распаковать целиком и запустить `skipkob_helper.exe`.
Папку держать целиком: рядом с exe лежат необходимые библиотеки.

## Запуск из исходников

```bash
flutter pub get
flutter run
```

Требуется Flutter 3.29+.

## Сборка

```bash
flutter build apk --release --build-name=X.Y.Z --build-number=N
```

```bash
flutter build windows --release
```

APK появится в `build/app/outputs/flutter-apk/`, Windows-сборка — в
`build/windows/x64/runner/Release/`.

`--build-number` обязан расти от сборки к сборке, иначе Android не поставит APK
поверх предыдущего как обновление.

## Хранение данных

Всё локально, никаких серверов и аккаунтов:

- **Windows:** `%APPDATA%\com.arkorij\skipkob_helper\data.json`
- **Android:** внутренний каталог приложения

Перенос между устройствами — через экспорт и импорт файла расписания.

## Документация

Журнал разработки — решения, устройство жестов, грабли и планы — в
[DEVELOPMENT.md](DEVELOPMENT.md).

## Структура проекта

```
lib/
├── main.dart                    точка входа, оболочка, физика свайпа
├── app_state.dart               общее состояние и уведомление страниц
├── core/                        логика без интерфейса
│   ├── models.dart              статусы, типы занятий, разбор строки пары
│   ├── calendar.dart            недели ↔ даты, чётность, каникулы
│   ├── resolver.dart            пары конкретного дня и отметки
│   ├── stats.dart               статистика, правило «полного дня», отчёт
│   └── store.dart               модель данных и JSON-хранилище
└── ui/
    ├── theme.dart               палитра, размеры, общие виджеты
    └── pages/
        ├── stats_page.dart      статистика
        ├── schedule_page.dart   расписание
        └── settings_page.dart   настройки
```
